class Solution:
    def buildArray(self, target: List[int], n: int) -> List[str]:
        res=[]
        i=1
        for num in target:
            while i<  um:
                res.append("push")
                res.append("pop")
                i+=1
            res.append("push")
            
            i+=1
        return res
