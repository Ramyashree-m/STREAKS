class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        seen=set()
        for num in nums:
            if nums in seen:
                return True
            seen.add(nums)
        return false
